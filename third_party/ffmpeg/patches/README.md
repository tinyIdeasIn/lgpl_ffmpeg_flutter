# FFmpeg Patches

Record any local patches applied to the approved LGPL FFmpeg source here.

Leave this directory empty only if the distributed dynamic libraries are built
from unmodified FFmpeg source.
